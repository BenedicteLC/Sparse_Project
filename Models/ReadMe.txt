-The output labels for all models are train_outputs.pkl and valid_outputs.pkl.
Both files contain a numpy array of outputs.
-The encoded inputs (hidden layers) are called validset200.pkl
(for the hidden layer of size 200), validset600.pkl, etc.
They contain a numpy 2D array. Each model has its own.